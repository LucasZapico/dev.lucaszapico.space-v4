---
title: "React18: Async Data Fetching Patterns"
description: "General notes, cheatsheet and reference Async Data Fetching Patterns"
date-created: 2023/09/12
last-modified: 2023/09/12
isdraft: false
categories: ["async", "react", "cheatsheet", "reference", "notes"]
tags: ["react", "cheatsheet", "reference", "notes"]
type: "note"
---

import { Box } from "@chakra-ui/react"
import { CodeSandbox } from "mdx-embed"

```ts
import { useCallback, useState, useEffect } from "react";
import "./styles.css";

export default function App() {
  const [fooData, setFooData] = useState<null | any>(null);

  const fetchData = useCallback(async () => {
    const res = await fetch("https://jsonplaceholder.typicode.com/posts/1");
    const data = await res.json();
    console.log(data);
    setFooData(data);
  });
  useEffect(() => {
    fetchData();
  }, []);
  return (
    <div className="App">
      {fooData ? (
        <div>
          <div>{fooData.userId}</div>
          <div>{fooData.title}</div>
          <div>{fooData.body}</div>
        </div>
      ) : (
        <div></div>
      )}
    </div>
  );
}
```


## Living Pattern Validations  

<Box as={CodeSandbox} mb={6} codeSandboxId="react18-async-data-fetching-patterns-v1-0-h6wd8s" ></Box>