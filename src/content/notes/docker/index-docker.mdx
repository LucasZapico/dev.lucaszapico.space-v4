---
title: "Docker"
description: ""
date-created: "2022/09/11"
last-modified: "2024/08/15"
categories: ["docker", "development", "dev-ops"]
tags: ["docker", "development", "dev-ops", "hosting", "notes"]
isdraft: true
type: "note"
---


## Setting up 

### Linux 

[apt install linux](https://docs.docker.com/engine/install/ubuntu/)

### Troubleshooting 

#### Permission Denied 

```
permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock: 
````


##### Solutions

1. reboot and log in again 

#### Check Docker Group 

```sh 
 sudo groupadd docker
```

```sh 
sudo usermod -aG docker $USER
```

```sh 
sudo reboot -n
```

#### Adjust permission on `/var/run/docker.sock`

```sh 
sudo chmod 666 /var/run/docker.sock

```


## TroubleShooting Cheat Sheet 

## Restart Docker 

Linux ubuntu

```sh 
sudo systemctl restart docker
```

Macos 

```sh 
osascript -e 'quit app "Docker"'

```
or 

```sh 
killall Docker\ Desktop
```

### Open docker 

Macos
```sh 
open /Applications/Docker.app
```

Linux ubuntu

```sh 
docker

```


List the image and container names with their associated ports in a clean table

```sh 
docker ps --format 'table{{.Image}}\t {{.Ports}}'
```

```sh
docker ps --format 'table{{.Image}}\t{{.Names}}\t{{.Ports}}'
```

Stops containers and removes containers, networks, volumes, and images created by `up`.

```sh
docker compose down
```

[Docker docs reference compose down](https://docs.docker.com/engine/reference/commandline/compose_down/)


```sh
docker volume prune
```

```sh 
docker system prune
```

Remove all unused containers, networks, images (both dangling and unreferenced), and optionally, volumes.

[Docker docs reference system prune](https://docs.docker.com/engine/reference/commandline/system_prune/)



## Reference

[Dockerfile cheatsheet - kapeli.com](https://kapeli.com/cheat_sheets/Dockerfile.docset/Contents/Resources/Documents/index)
